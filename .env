# Server configuration
SERVER_PORT=8080

# MongoDB connection
MONGO_URI=mongodb://mongodb:27017
DB_NAME=muchtodo

# JWT / Auth
JWT_SECRET_KEY=dev-secret
JWT_EXPIRATION_HOURS=24

# Caching (if your app uses it)
ENABLE_CACHE=false

